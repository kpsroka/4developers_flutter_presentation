
import 'package:flutter/material.dart';

class DemoTime extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('Demo time!', style: Theme.of(context).textTheme.display4);
  }
}